# Build directory (CMake artifacts + compiled binaries)
build/

# Compiled binaries at root level
sci-oh
sci-oh.exe

# Object files / static libs
*.o
*.a
*.so
*.dylib

# Environment / secrets
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db

# Editors / IDEs
.vscode/
.idea/
*.swp
*~

# Docker (local dev configs)
Dockerfile*

# Claude Code per-user overrides (project settings.json is tracked)
.claude/settings.local.json
